local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = playerGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false  -- вот это основное изменение


local yellow = Color3.fromRGB(255, 223, 0)
local blue = Color3.fromRGB(0, 51, 204)
local green = Color3.fromRGB(0, 170, 0)
local darkGreenText = Color3.fromRGB(0, 100, 0)
local darkBlueText = Color3.fromRGB(0, 34, 102)

local function styleButton(btn)
    btn.BackgroundColor3 = yellow
    btn.Font = Enum.Font.GothamBold
    btn.TextColor3 = darkGreenText
    btn.TextScaled = true
    btn.TextWrapped = true
    btn.AutoButtonColor = false
    local border = Instance.new("UIStroke", btn)
    border.Color = green
    border.Thickness = 2
    btn.MouseEnter:Connect(function()
        btn.BackgroundColor3 = green
        border.Color = yellow
        btn.TextColor3 = Color3.new(1, 1, 1)
    end)
    btn.MouseLeave:Connect(function()
        btn.BackgroundColor3 = yellow
        border.Color = green
        btn.TextColor3 = darkGreenText
    end)
end

local function styleTextBox(box)
    box.BackgroundColor3 = yellow
    box.Font = Enum.Font.GothamBold
    box.TextColor3 = darkGreenText
    box.TextScaled = true
    box.TextWrapped = true
    local border = Instance.new("UIStroke", box)
    border.Color = green
    border.Thickness = 2
    box.ClearTextOnFocus = false
end

local function createButton(name, text, pos, size, parent)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Parent = parent
    btn.Position = pos
    btn.Size = size
    btn.Text = text
    styleButton(btn)
    return btn
end

local AGButton = Instance.new("TextButton")
AGButton.Name = "AGButton"
AGButton.Parent = ScreenGui
AGButton.Size = UDim2.new(0, 60, 0, 35)
AGButton.Position = UDim2.new(1, -70, 0, 15)
AGButton.Text = "AG"
styleButton(AGButton)
AGButton.ZIndex = 10

local Frame

local function clampPosition(frame)
    local absPos = frame.AbsolutePosition
    local absSize = frame.AbsoluteSize
    local screenSize = workspace.CurrentCamera.ViewportSize
    local x = math.clamp(absPos.X, 0, screenSize.X - absSize.X)
    local y = math.clamp(absPos.Y, 0, screenSize.Y - absSize.Y)
    frame.Position = UDim2.new(0, x, 0, y)
end

local function dragify(frame)
    local UIS = game:GetService("UserInputService")
    local dragging = false
    local dragInput, dragStart, startPos

    local function update(input)
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            0,
            math.clamp(startPos.X.Offset + delta.X, 0, workspace.CurrentCamera.ViewportSize.X - frame.AbsoluteSize.X),
            0,
            math.clamp(startPos.Y.Offset + delta.Y, 0, workspace.CurrentCamera.ViewportSize.Y - frame.AbsoluteSize.Y)
        )
    end

    local function startDrag(input)
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                clampPosition(frame)
            end
        end)
    end

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            startDrag(input)
        end
    end)

    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            update(input)
        end
    end)
end

local fullBrightActive = false
local originalLighting = {
    Brightness = game.Lighting.Brightness,
    ClockTime = game.Lighting.ClockTime or 14,
    FogEnd = game.Lighting.FogEnd,
    FogColor = game.Lighting.FogColor,
    Ambient = game.Lighting.Ambient,
    OutdoorAmbient = game.Lighting.OutdoorAmbient,
}

local function setFullBright(state)
    if state then
        game.Lighting.Brightness = 2
        game.Lighting.ClockTime = 14
        game.Lighting.FogEnd = 100000
        game.Lighting.FogColor = Color3.new(1, 1, 1)
        game.Lighting.Ambient = Color3.new(1, 1, 1)
        game.Lighting.OutdoorAmbient = Color3.new(1, 1, 1)
    else
        for k,v in pairs(originalLighting) do
            game.Lighting[k] = v
        end
    end
end

AGButton.MouseButton1Click:Connect(function()
    if Frame and Frame.Parent then
        Frame.Visible = not Frame.Visible
        return
    end

    Frame = Instance.new("Frame")
    Frame.Name = "MainGuiFrame"
    Frame.Parent = ScreenGui
    Frame.BackgroundColor3 = blue
    Frame.Size = UDim2.new(0, 550, 0, 350)
    Frame.Position = UDim2.new(0.5, -275, 0.3, 0)
    Frame.ZIndex = 10

    local Title = Instance.new("TextLabel")
    Title.Parent = Frame
    Title.BackgroundColor3 = yellow
    Title.Size = UDim2.new(1, 0, 0, 30)
    Title.Font = Enum.Font.GothamBold
    Title.Text = "Arxetector GUI"
    Title.TextColor3 = darkBlueText
    Title.TextScaled = true
    local titleStroke = Instance.new("UIStroke", Title)
    titleStroke.Color = green
    titleStroke.Thickness = 3

    local padding = 15
    local btnWidth = 160
    local btnHeight = 40
    local spacingY = 15

    local function addButton(text, pos)
        local btn = createButton(text:gsub("%s", ""), text, pos, UDim2.new(0, btnWidth, 0, btnHeight), Frame)
        return btn
    end

    local flingBtn = addButton("Fling", UDim2.new(0, padding, 0, 40))
    flingBtn.MouseButton1Click:Connect(function()
        pcall(function()
            loadstring(game:HttpGet("https://pastebin.com/raw/TvGvMSYf"))()
        end)
    end)

    local infiniteJumpActive = false
    local UIS = game:GetService("UserInputService")
    local infJumpConn
    local infJumpBtn = addButton("Бесконечный прыжок", UDim2.new(0, padding + btnWidth + 20, 0, 40))
    infJumpBtn.MouseButton1Click:Connect(function()
        infiniteJumpActive = not infiniteJumpActive
        infJumpBtn.Text = infiniteJumpActive and "Бесконечный прыжок ВКЛ" or "Бесконечный прыжок"
        if infiniteJumpActive then
            infJumpConn = UIS.JumpRequest:Connect(function()
                local char = player.Character
                if char and char:FindFirstChildOfClass("Humanoid") then
                    char.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        else
            if infJumpConn then
                infJumpConn:Disconnect()
                infJumpConn = nil
            end
        end
    end)

    local speedHackActive = false
    local defaultWalkSpeed = 16
    local speedMultiplier = 2.5
    local speedBtn = addButton("Ускорение х2.5", UDim2.new(0, padding, 0, 40 + btnHeight + spacingY))
    speedBtn.MouseButton1Click:Connect(function()
        speedHackActive = not speedHackActive
        local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if speedHackActive then
            if humanoid then humanoid.WalkSpeed = defaultWalkSpeed * speedMultiplier end
            speedBtn.Text = "Ускорение ВКЛ"
        else
            if humanoid then humanoid.WalkSpeed = defaultWalkSpeed end
            speedBtn.Text = "Ускорение х2.5"
        end
    end)

    local nightBtn = addButton("FullBright ВЫКЛ", UDim2.new(0, padding + btnWidth + 20, 0, 40 + btnHeight + spacingY))
    nightBtn.MouseButton1Click:Connect(function()
        fullBrightActive = not fullBrightActive
        setFullBright(fullBrightActive)
        nightBtn.Text = fullBrightActive and "FullBright ВКЛ" or "FullBright ВЫКЛ"
    end)

    local tpNameBtn = addButton("Телепорт по нику", UDim2.new(0, padding, 0, 40 + 2*(btnHeight + spacingY)))
    local tpNameBox = Instance.new("TextBox")
    tpNameBox.Parent = Frame
    tpNameBox.Size = UDim2.new(0, btnWidth, 0, btnHeight)
    tpNameBox.Position = UDim2.new(0, padding, 0, 40 + 3*(btnHeight + spacingY))
    tpNameBox.PlaceholderText = "Ник игрока"
    styleTextBox(tpNameBox)

    tpNameBtn.MouseButton1Click:Connect(function()
        local input = tpNameBox.Text:lower()
        if input ~= "" then
            for _,plr in pairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Name:lower():find(input) then
                    if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = plr.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0)
                    end
                    break
                end
            end
        end
    end)

  local razborBtn = addButton("Разбор", UDim2.new(0, padding + btnWidth + 20, 0, 40 + 2*(btnHeight + spacingY)))

razborBtn.MouseButton1Click:Connect(function()
    local player = game.Players.LocalPlayer
    local playerGui = player:WaitForChild("PlayerGui")
    
    -- Если уже есть CheskCode — удаляем его (тоггл)
    local existing = playerGui:FindFirstChild("CheskCode")
    if existing then
        existing:Destroy()
        return
    end

    -- Создаём новый CheskCode
    local CheskCode = Instance.new("ScreenGui")
    CheskCode.Name = "CheskCode"
    CheskCode.Parent = playerGui
    CheskCode.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local yellow = Color3.fromRGB(255, 223, 0)
    local blue = Color3.fromRGB(0, 51, 204)
    local green = Color3.fromRGB(0, 170, 0)
    local darkGreenText = Color3.fromRGB(0, 100, 0)

    local function styleButton(btn)
        btn.BackgroundColor3 = yellow
        btn.Font = Enum.Font.GothamBold
        btn.TextColor3 = darkGreenText
        btn.TextScaled = true
        btn.TextWrapped = true
        btn.AutoButtonColor = false
        local border = Instance.new("UIStroke", btn)
        border.Color = green
        border.Thickness = 2
        btn.MouseEnter:Connect(function()
            btn.BackgroundColor3 = green
            border.Color = yellow
            btn.TextColor3 = Color3.new(1,1,1)
        end)
        btn.MouseLeave:Connect(function()
            btn.BackgroundColor3 = yellow
            border.Color = green
            btn.TextColor3 = darkGreenText
        end)
    end

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 300, 0, 100)
    frame.Position = UDim2.new(0.5, -150, 0.5, -50)
    frame.BackgroundColor3 = blue
    frame.Parent = CheskCode

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1,0,0,30)
    title.Position = UDim2.new(0,0,0,0)
    title.Text = "CheskCode"
    title.Font = Enum.Font.GothamBold
    title.TextScaled = true
    title.TextColor3 = yellow
    title.BackgroundColor3 = green
    title.Parent = frame

    local function createButton(text, pos, parent, url)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 120, 0, 40)
        btn.Position = pos
        btn.Text = text
        btn.Parent = parent
        styleButton(btn)

        btn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet(url))()
        end)
    end

    -- Кнопки GUI и OBJ
    createButton("GUI", UDim2.new(0, 10, 0, 50), frame, "https://pastebin.com/raw/03rEixHy")
    createButton("OBJ", UDim2.new(0, 160, 0, 50), frame, "https://pastebin.com/raw/hT2kriXp")
end)


    local invisBtn = addButton("Брукхевен краш", UDim2.new(0, padding + btnWidth + 20, 0, 40 + 3*(btnHeight + spacingY)))
    invisBtn.MouseButton1Click:Connect(function()
        pcall(function()
         loadstring(game:HttpGet('https://raw.githubusercontent.com/M1ZZ001/BrookhavenR4D/main/Brookhaven%20R4D%20Script'))()
        end)
    end)

    local partOrbit = addButton("Part Orbit", UDim2.new(0, padding + 2*(btnWidth + 20), 0, 40))
    partOrbit.MouseButton1Click:Connect(function()
        pcall(function()
            loadstring(game:HttpGet("https://pastebin.com/raw/aZjaAr6F", true))()
        end)
    end)

    local spooky = Instance.new("Sound")
    spooky.SoundId = "rbxassetid://95156028272944"
    spooky.Volume = 3
    spooky.PlaybackSpeed = 0.2
    spooky.Looped = false
    spooky.Parent = workspace

    local itSound = Instance.new("Sound")
    itSound.SoundId = "rbxassetid://142376088"
    itSound.Volume = 3
    itSound.PlaybackSpeed = 1
    itSound.Looped = false
    itSound.Parent = workspace

    local customSound = Instance.new("Sound")
    customSound.Volume = 3
    customSound.Parent = workspace

    local lastClick = {}
    local DOUBLE = 0.5

    local function toggleSound(sound, key)
        local now = tick()
        if lastClick[key] and now - lastClick[key] < DOUBLE then
            if sound and sound.Playing then sound:Stop() end
        else
            if sound then sound:Play() end
        end
        lastClick[key] = now
    end

    local musicBtn = addButton("Музыка", UDim2.new(0, padding + 2*(btnWidth + 20), 0, 40 + btnHeight + spacingY))
    musicBtn.MouseButton1Click:Connect(function()
        if musicFrame then
            musicFrame.Visible = not musicFrame.Visible
            return
        end
        musicFrame = Instance.new("Frame")
        musicFrame.Size = UDim2.new(0, 300, 0, 200)
        musicFrame.Position = UDim2.new(0.5, -150, 0.5, -100)
        musicFrame.BackgroundColor3 = Color3.fromRGB(0,0,0)
        musicFrame.Parent = Frame

        local function createMusicButton(text,pos,sound,key)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0,280,0,40)
            btn.Position = pos
            btn.Text = text
            btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
            btn.TextColor3 = Color3.fromRGB(255,255,255)
            btn.Parent = musicFrame
            btn.MouseButton1Click:Connect(function()
                toggleSound(sound,key)
            end)
            return btn
        end

        createMusicButton("Spooky Scary Skeletons", UDim2.new(0,10,0,10), spooky, "spooky")
        createMusicButton("Raining Tacos (IT)", UDim2.new(0,10,0,60), itSound, "it")

        local customBox = Instance.new("TextBox", musicFrame)
        customBox.Size = UDim2.new(0,280,0,40)
        customBox.Position = UDim2.new(0,10,0,110)
        customBox.PlaceholderText = "Custom Sound ID"
        local customBtn = createMusicButton("Play/Stop Custom", UDim2.new(0,10,0,160), customSound, "custom")
        customBtn.MouseButton1Click:Connect(function()
            if customBox.Text ~= "" then
                customSound.SoundId = "rbxassetid://"..customBox.Text
            end
            toggleSound(customSound,"custom")
        end)
    end)

    local test3 = addButton("DANCE", UDim2.new(0, padding + 2*(btnWidth + 20), 0, 40 + 2*(btnHeight + spacingY)))
    test3.MouseButton1Click:Connect(function()
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:WaitForChild("Humanoid")
        local animation = Instance.new("Animation")
        animation.AnimationId = "rbxassetid://27789359"
        local track = humanoid:LoadAnimation(animation)
        track:Play()
    end)

    local HttpService = game:GetService("HttpService")
    local TeleportService = game:GetService("TeleportService")
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local PlaceId = game.PlaceId
    local JobId = game.JobId

    local test4 = addButton("Serer Hop", UDim2.new(0, padding + 2*(btnWidth + 20), 0, 40 + 3*(btnHeight + spacingY)))
    test4.MouseButton1Click:Connect(function()
        spawn(function()
            local servers = {}
            local cursor = ""
            repeat
                local success, response = pcall(function()
                    return game:HttpGet("https://games.roblox.com/v1/games/"..PlaceId.."/servers/Public?sortOrder=Asc&limit=100"..(cursor ~= "" and "&cursor="..cursor or ""))
                end)
                if not success then return end
                local data
                local ok = pcall(function()
                    data = HttpService:JSONDecode(response)
                end)
                if not ok or type(data) ~= "table" or type(data.data) ~= "table" then return end
                for _, v in pairs(data.data) do
                    if v.playing < v.maxPlayers and v.id ~= JobId then
                        table.insert(servers, v.id)
                    end
                end
                cursor = data.nextPageCursor
            until #servers > 0 or not cursor
            if #servers > 0 then
                local choice = servers[math.random(1, #servers)]
                TeleportService:TeleportToPlaceInstance(PlaceId, choice, LocalPlayer)
            end
        end)
    end)

    local footer = Instance.new("TextLabel")
    footer.Parent = Frame
    footer.Size = UDim2.new(1, 0, 0, 25)
    footer.Position = UDim2.new(0, 0, 1, -25)
    footer.BackgroundColor3 = yellow
    footer.Font = Enum.Font.GothamBold
    footer.Text = "By Arxetector"
    footer.TextColor3 = darkBlueText
    footer.TextScaled = true
    local footerStroke = Instance.new("UIStroke", footer)
    footerStroke.Color = green
    footerStroke.Thickness = 2

    dragify(Frame)

local superToysBtn = addButton("Super Toys", UDim2.new(0, padding, 0, 40 + 4*(btnHeight + spacingY)))
superToysBtn.MouseButton1Click:Connect(function()
    local Players = game:GetService("Players")
    local lp = Players.LocalPlayer
    local mouse = lp:GetMouse()
    local Workspace = game:GetService("Workspace")

    -- TP Stick
    local tool = Instance.new("Tool")
    tool.Name = "TP Stick"
    tool.RequiresHandle = true
    tool.Parent = lp.Backpack

    local handle = Instance.new("Part")
    handle.Name = "Handle"
    handle.Size = Vector3.new(0.3, 3, 0.3)
    handle.Color = Color3.fromRGB(255, 255, 0)
    handle.Material = Enum.Material.Neon
    handle.CanCollide = false
    handle.Anchored = false
    handle.Parent = tool

    local tip = Instance.new("Part")
    tip.Size = Vector3.new(0.5,0.5,0.5)
    tip.Shape = Enum.PartType.Ball
    tip.Color = Color3.fromRGB(255,255,0)
    tip.Material = Enum.Material.Neon
    tip.Anchored = false
    tip.CanCollide = false
    tip.Parent = tool
    tip.CFrame = handle.CFrame + Vector3.new(0,1.5,0)

    tool.Activated:Connect(function()
        local char = lp.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local targetPos = mouse.Hit.Position + Vector3.new(0,3,0)
        char.HumanoidRootPart.CFrame = CFrame.new(targetPos)
    end)

    -- Portal Wands
    local portalPoints = {}

    local function createWand(name)
        local wand = Instance.new("Tool")
        wand.Name = name
        wand.RequiresHandle = false
        wand.Parent = lp.Backpack
        return wand
    end

    local setWand = createWand("Set Wand")
    local deleteWand = createWand("Delite Wand")
    local teleportWand = createWand("Teleport Wand")

    local function createPortalPoint(position)
        local part = Instance.new("Part")
        part.Size = Vector3.new(2,2,2)
        part.Position = position
        part.Anchored = true
        part.CanCollide = false
        part.BrickColor = BrickColor.new("Bright yellow")
        part.Shape = Enum.PartType.Ball
        part.Parent = Workspace
        return part
    end

    setWand.Activated:Connect(function()
        local targetPos = mouse.Hit.Position + Vector3.new(0,2,0)
        local newPart = createPortalPoint(targetPos)
        table.insert(portalPoints, newPart)
        if #portalPoints > 2 then
            portalPoints[1]:Destroy()
            table.remove(portalPoints, 1)
        end
    end)

    deleteWand.Activated:Connect(function()
        local lastPart = table.remove(portalPoints)
        if lastPart then lastPart:Destroy() end
    end)

    teleportWand.Activated:Connect(function()
        if #portalPoints < 2 then
            warn("Нужно минимум две точки!")
            return
        end
        local char = lp.Character
        if not char or not char.PrimaryPart then return end

        local pos = char.PrimaryPart.Position
        local dist1 = (portalPoints[1].Position - pos).Magnitude
        local dist2 = (portalPoints[2].Position - pos).Magnitude

        local targetPoint = dist1 < dist2 and portalPoints[2] or portalPoints[1]
        char:SetPrimaryPartCFrame(CFrame.new(targetPoint.Position + Vector3.new(0,3,0)))
    end)
end)

 local probivBtn = addButton("Probiv", UDim2.new(0, padding + btnWidth + 20, 0, 40 + 4*(btnHeight + spacingY)))
probivBtn.MouseButton1Click:Connect(function()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local MarketplaceService = game:GetService("MarketplaceService")
    local playerGui = LocalPlayer:WaitForChild("PlayerGui")

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Parent = playerGui
    ScreenGui.Name = "PassportGUI"

    local yellow = Color3.fromRGB(255, 223, 0)
    local blue = Color3.fromRGB(0, 51, 204)
    local green = Color3.fromRGB(0, 170, 0)
    local darkGreenText = Color3.fromRGB(0, 100, 0)
    local darkBlueText = Color3.fromRGB(0, 34, 102)
    local whiteText = Color3.new(1,1,1)

    local function styleButton(btn)
        btn.BackgroundColor3 = yellow
        btn.Font = Enum.Font.GothamBold
        btn.TextColor3 = darkGreenText
        btn.TextScaled = true
        btn.TextWrapped = true
        btn.AutoButtonColor = false
        local border = Instance.new("UIStroke", btn)
        border.Color = green
        border.Thickness = 2
        local corner = Instance.new("UICorner", btn)
        corner.CornerRadius = UDim.new(0,8)
        btn.MouseEnter:Connect(function()
            btn.BackgroundColor3 = green
            border.Color = yellow
            btn.TextColor3 = whiteText
        end)
        btn.MouseLeave:Connect(function()
            btn.BackgroundColor3 = yellow
            border.Color = green
            btn.TextColor3 = darkGreenText
        end)
    end

    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(0, 450, 0, 400)
    Frame.Position = UDim2.new(0.5, -225, 0.3, 0)
    Frame.BackgroundColor3 = blue
    Frame.Parent = ScreenGui
    Frame.Active = true
    Frame.Draggable = true
    local frameCorner = Instance.new("UICorner", Frame)
    frameCorner.CornerRadius = UDim.new(0,12)
    local frameStroke = Instance.new("UIStroke", Frame)
    frameStroke.Color = green
    frameStroke.Thickness = 3

    local Title = Instance.new("TextLabel")
    Title.Parent = Frame
    Title.Size = UDim2.new(1,0,0,50)
    Title.Position = UDim2.new(0,0,0,0)
    Title.BackgroundColor3 = yellow
    Title.Font = Enum.Font.GothamBlack
    Title.Text = "Паспорт игрока"
    Title.TextColor3 = darkBlueText
    Title.TextScaled = true
    Title.TextWrapped = true
    local titleStroke = Instance.new("UIStroke", Title)
    titleStroke.Color = green
    titleStroke.Thickness = 3
    local titleShadow = Instance.new("UICorner", Title)
    titleShadow.CornerRadius = UDim.new(0,10)

    local NameBox = Instance.new("TextBox")
    NameBox.Parent = Frame
    NameBox.Size = UDim2.new(0, 250, 0, 35)
    NameBox.Position = UDim2.new(0, 20, 0, 70)
    NameBox.PlaceholderText = "Введите ник игрока"
    NameBox.Font = Enum.Font.GothamBold
    NameBox.TextColor3 = darkGreenText
    NameBox.TextScaled = true
    NameBox.TextWrapped = true
    NameBox.BackgroundColor3 = yellow
    local nameStroke = Instance.new("UIStroke", NameBox)
    nameStroke.Color = green
    nameStroke.Thickness = 2
    local nameCorner = Instance.new("UICorner", NameBox)
    nameCorner.CornerRadius = UDim.new(0,6)

    local ShowBtn = Instance.new("TextButton")
    ShowBtn.Parent = Frame
    ShowBtn.Size = UDim2.new(0, 150, 0, 35)
    ShowBtn.Position = UDim2.new(0, 290, 0, 70)
    ShowBtn.Text = "Показать"
    styleButton(ShowBtn)

    local InfoLabel = Instance.new("TextLabel")
    InfoLabel.Parent = Frame
    InfoLabel.Size = UDim2.new(1, -20, 0, 160)
    InfoLabel.Position = UDim2.new(0, 10, 0, 120)
    InfoLabel.Text = ""
    InfoLabel.TextWrapped = true
    InfoLabel.TextScaled = true
    InfoLabel.Font = Enum.Font.GothamBold
    InfoLabel.TextColor3 = yellow
    InfoLabel.BackgroundTransparency = 1

    local AvatarImg = Instance.new("ImageLabel")
    AvatarImg.Parent = Frame
    AvatarImg.Size = UDim2.new(0,120,0,120)
    AvatarImg.Position = UDim2.new(0.5, -60, 0, 290)
    AvatarImg.BackgroundTransparency = 1
    local avatarCorner = Instance.new("UICorner", AvatarImg)
    avatarCorner.CornerRadius = UDim.new(0,60)
    local avatarStroke = Instance.new("UIStroke", AvatarImg)
    avatarStroke.Color = green
    avatarStroke.Thickness = 2

    local function findPlayerByFirstLetter(letter)
        letter = letter:lower()
        for _, player in pairs(Players:GetPlayers()) do
            if player.Name:lower():sub(1,1) == letter then
                return player
            end
        end
        return nil
    end

    local function getExecutorGameName()
        local success, info = pcall(function()
            return MarketplaceService:GetProductInfo(game.PlaceId)
        end)
        if success and info then
            return info.Name
        else
            return "Не удалось определить название"
        end
    end

    ShowBtn.MouseButton1Click:Connect(function()
        local inputText = NameBox.Text
        if inputText == "" then return end
        local firstLetter = inputText:sub(1,1)
        local target = findPlayerByFirstLetter(firstLetter)
        if target then
            local userId = target.UserId
            local thumbUrl = Players:GetUserThumbnailAsync(userId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
            AvatarImg.Image = thumbUrl
            local gameName = getExecutorGameName()
            InfoLabel.Text = "Ник: "..target.Name.."\nUserId: "..userId.."\nИгра: "..gameName
        else
            InfoLabel.Text = "Игрок не найден!"
            AvatarImg.Image = ""
        end
    end)

    local TeleportBtn = Instance.new("TextButton")
    TeleportBtn.Parent = Frame
    TeleportBtn.Size = UDim2.new(0, 180, 0, 40)
    TeleportBtn.Position = UDim2.new(0.5, -90, 0, 430)
    TeleportBtn.Text = "Телепорт к игроку"
    styleButton(TeleportBtn)

    TeleportBtn.MouseButton1Click:Connect(function()
        local inputText = NameBox.Text
        if inputText == "" then
            InfoLabel.Text = "Введите ник игрока для телепорта!"
            return
        end
        local firstLetter = inputText:sub(1,1)
        local target = findPlayerByFirstLetter(firstLetter)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0)
            InfoLabel.Text = "Телепорт выполнен к игроку: "..target.Name
        else
            InfoLabel.Text = "Невозможно телепортироваться к игроку!"
        end
    end)
end)


local execToggleBtn = addButton("Arxetector", UDim2.new(0, padding + 2*(btnWidth + 20), 0, 40 + 4*(btnHeight + spacingY)))

local executorGui
execToggleBtn.MouseButton1Click:Connect(function()
    if executorGui and executorGui.Parent then
        executorGui.Enabled = not executorGui.Enabled
        return
    end

    executorGui = Instance.new("ScreenGui")
    executorGui.Name = "ArxetectorExecutor"
    executorGui.Parent = game.CoreGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 420, 0, 300)
    mainFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
    mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    mainFrame.BorderSizePixel = 0
    mainFrame.Active = true
    mainFrame.Draggable = true
    mainFrame.Parent = executorGui

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 14)
    UICorner.Parent = mainFrame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 40)
    title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    title.Text = "⚡ Arxetector Executor ⚡"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 18
    title.Parent = mainFrame

    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 14)
    titleCorner.Parent = title

    local codeBox = Instance.new("TextBox")
    codeBox.Size = UDim2.new(1, -20, 0, 140)
    codeBox.Position = UDim2.new(0, 10, 0, 50)
    codeBox.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    codeBox.TextColor3 = Color3.fromRGB(220, 220, 220)
    codeBox.TextSize = 15
    codeBox.Font = Enum.Font.Code
    codeBox.ClearTextOnFocus = false
    codeBox.MultiLine = true
    codeBox.TextXAlignment = Enum.TextXAlignment.Left
    codeBox.TextYAlignment = Enum.TextYAlignment.Top
    codeBox.Text = "-- Вставь сюда свой скрипт"
    codeBox.Parent = mainFrame

    local codeCorner = Instance.new("UICorner")
    codeCorner.CornerRadius = UDim.new(0, 8)
    codeCorner.Parent = codeBox

    local logBox = Instance.new("TextLabel")
    logBox.Size = UDim2.new(1, -20, 0, 40)
    logBox.Position = UDim2.new(0, 10, 0, 200)
    logBox.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    logBox.TextColor3 = Color3.fromRGB(200, 200, 200)
    logBox.TextSize = 14
    logBox.Font = Enum.Font.Code
    logBox.TextXAlignment = Enum.TextXAlignment.Left
    logBox.TextYAlignment = Enum.TextYAlignment.Center
    logBox.Text = "💻 Logs: Ожидание..."
    logBox.Parent = mainFrame

    local logCorner = Instance.new("UICorner")
    logCorner.CornerRadius = UDim.new(0, 8)
    logCorner.Parent = logBox

    local function makeButton(txt, color, pos, emoji)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 100, 0, 35)
        btn.Position = pos
        btn.BackgroundColor3 = color
        btn.Text = (emoji or "").." "..txt
        btn.TextColor3 = Color3.new(1,1,1)
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 16
        btn.AutoButtonColor = false
        btn.Parent = mainFrame

        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 8)
        btnCorner.Parent = btn

        btn.MouseEnter:Connect(function()
            btn.BackgroundColor3 = color:lerp(Color3.new(1,1,1), 0.2)
        end)
        btn.MouseLeave:Connect(function()
            btn.BackgroundColor3 = color
        end)

        return btn
    end

    -- Кнопки внизу
    local execBtn = makeButton("Execute", Color3.fromRGB(0, 170, 0), UDim2.new(0, 10, 1, -45))
    local clearBtn = makeButton("Clear", Color3.fromRGB(170, 0, 0), UDim2.new(0, 120, 1, -45), "🧹")
    local scriptHubBtn = makeButton("Script Hub", Color3.fromRGB(255, 215, 0), UDim2.new(0, 230, 1, -45), "📜")

-- Close кнопка чуть больше
local closeBtn = makeButton("❌", Color3.fromRGB(60, 60, 60), UDim2.new(0, 340, 1, -45))
closeBtn.Size = UDim2.new(0, 70, 0, 35)  -- шире, чем 50, но всё ещё компактно



    -- Script Hub
    local scriptHubGui
    scriptHubBtn.MouseButton1Click:Connect(function()
        if scriptHubGui and scriptHubGui.Parent then
            scriptHubGui.Enabled = not scriptHubGui.Enabled
            return
        end

        scriptHubGui = Instance.new("ScreenGui")
        scriptHubGui.Name = "ScriptHub"
        scriptHubGui.Parent = game.CoreGui

        local hubFrame = Instance.new("Frame")
        hubFrame.Size = UDim2.new(0, 400, 0, 300)
        hubFrame.Position = UDim2.new(0.35, 0, 0.1, 0)
        hubFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        hubFrame.BorderSizePixel = 0
        hubFrame.Active = true
        hubFrame.Draggable = true
        hubFrame.Parent = scriptHubGui

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 14)
        corner.Parent = hubFrame

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 40)
        title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        title.Text = "📜 Script Hub"
        title.TextColor3 = Color3.fromRGB(255, 255, 255)
        title.Font = Enum.Font.GothamBold
        title.TextSize = 18
        title.Parent = hubFrame

        local titleCorner = Instance.new("UICorner")
        titleCorner.CornerRadius = UDim.new(0, 14)
        titleCorner.Parent = title

        local function makeHubButton(txt, url, pos)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0, 180, 0, 35)
            btn.Position = pos
            btn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
            btn.Text = txt
            btn.TextColor3 = Color3.new(0,0,0)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 14
            btn.AutoButtonColor = false
            btn.Parent = hubFrame

            local btnCorner = Instance.new("UICorner")
            btnCorner.CornerRadius = UDim.new(0, 8)
            btnCorner.Parent = btn

            btn.MouseEnter:Connect(function()
                btn.BackgroundColor3 = btn.BackgroundColor3:lerp(Color3.new(1,1,1), 0.2)
            end)
            btn.MouseLeave:Connect(function()
                btn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
            end)

            btn.MouseButton1Click:Connect(function()
                local success, err = pcall(function()
                    loadstring(game:HttpGet(url))()
                end)
                if not success then
                    warn("Ошибка при запуске скрипта: "..err)
                end
                scriptHubGui.Enabled = false
            end)
        end

        -- Скрипты
        makeHubButton("MM2 Скрипт", "https://pastebin.com/raw/D1YBHUYn", UDim2.new(0, 10, 0, 60))
        makeHubButton("Универсальный", "https://pastebin.com/raw/aF4utt1S", UDim2.new(0, 210, 0, 60))
        makeHubButton("Админ телепорт", "https://pastebin.com/raw/G5ygdeFJ", UDim2.new(0, 10, 0, 110))
        makeHubButton("Игра в кальмара", "https://pastebin.com/raw/FFgQd3sm", UDim2.new(0, 210, 0, 110))
        makeHubButton("Форслайн", "https://pastebin.com/raw/uaDrANJA", UDim2.new(0, 10, 0, 160))
        makeHubButton("Зен Хаб Фортлайн", "https://raw.githubusercontent.com/ZenHub103/Zen-Hub-/refs/heads/main/Fortline", UDim2.new(0, 210, 0, 160))
        makeHubButton("Мардер Вортекс", "https://raw.githubusercontent.com/vertex-peak/vertex/refs/heads/main/loadstring", UDim2.new(0, 10, 0, 210))
    end)

    -- Execute
    execBtn.MouseButton1Click:Connect(function()
        local code = codeBox.Text
        if loadstring then
            local func, err = loadstring(code)
            if func then
                local success, runtimeErr = pcall(func)
                if success then
                    logBox.Text = "✅ Успешно выполнено!"
                    logBox.TextColor3 = Color3.fromRGB(0, 255, 0)
                else
                    logBox.Text = "⚠ Ошибка выполнения: " .. runtimeErr
                    logBox.TextColor3 = Color3.fromRGB(255, 100, 100)
                end
            else
                logBox.Text = "❌ Ошибка в коде: " .. err
                logBox.TextColor3 = Color3.fromRGB(255, 100, 100)
            end
        else
            logBox.Text = "⚠ loadstring не поддерживается!"
            logBox.TextColor3 = Color3.fromRGB(255, 255, 0)
        end
    end)

    -- Clear
    clearBtn.MouseButton1Click:Connect(function()
        codeBox.Text = ""
        logBox.Text = "💻 Logs: очищено."
        logBox.TextColor3 = Color3.fromRGB(200, 200, 200)
    end)

    -- Close
    closeBtn.MouseButton1Click:Connect(function()
        executorGui:Destroy()
    end)
end)


end)
